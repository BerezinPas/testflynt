{% extends 'templates/_document.twig' %}

{% block content %}
  {{ renderComponent('BreakingArticle') }}
  {{ renderComponent('SearchHeader') }}
  {{ renderComponent('SearchResults', { posts: posts }) }}
{% endblock %}
