{% extends 'templates/_document.twig' %}

{% block content %}
  <section class="site-content">
    <article class="site-article">
      <section class="post-content">
        <h1>{{ post.title }}</h1>
        {{ post.field_textComponents_content }}
      </section>
    </article>
  </section>
{% endblock %}
